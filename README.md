# genai-post-generator
Generative AI application that creates LinkedIn posts by learning a user’s writing patterns from existing content. It analyzes tone, structure, language, and length, then uses prompt engineering and few-shot learning with an LLM to generate new, style-consistent posts via a Python and Streamlit interface.
